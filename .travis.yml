language: rust

rust:
  - stable
  - nightly

before_install:
 - git submodule add https://github.com/rust-av/rust-av
 - git submodule update --init --recursive

script: 
 - cargo build --release --verbose
 - cargo test --release --verbose
