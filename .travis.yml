language: rust

rust:
  - stable
  - nightly

before_install:
 - cd /home/travis/build
 - git clone https://github.com/rust-av/rust-av.git
 - git submodule update --init --recursive

script: 
 - cargo build --release --verbose
 - cargo test --release --verbose
